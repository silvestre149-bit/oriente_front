export const formSemestre = {
    titulo: String,
    local: String,
    dataInicio: String,
    dataFim: String
}

export const formProfessor = {
    nome: String,
    cod: String,
    email: String,
}

export const formAluno = {
    nome: String,
    cod: String,
    turma: String,
}

export const formSessao = {
    data: String,
    local: String,
    horario: String,
    apresentacao: String,
    quantidade: Number
}